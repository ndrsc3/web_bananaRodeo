{
    "outputDirectory": "dist",
    "buildCommand": "npm run build",
    "routes": [
        { "src": "/api/(.*)", "dest": "/api/$1" },
        { "handle": "filesystem" }
    ],
    "functions": {
        "api/*.ts": { "runtime": "@vercel/node" }
    }
}
