{
  "name": "banana.rodeo",
  "version": "1.0.0",
  "description": "A website",
  "main": "dist/js/main.js",
  "scripts": {
    "clean": "rm -rf dist/*",
    "copy-assets": "cp -r css assets dist/",
    "build": "npm run clean && tsc && npm run copy-assets && node dist/js/build.js",
    "vercel-build": "npm run build",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
