{
  "name": "@banana-rodeo/client",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && node dist/build.js",
    "clean": "rimraf dist && rimraf ../../.vercel/output/static",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@banana-rodeo/shared": "1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "rimraf": "^5.0.5",
    "@types/node": "^20.11.0"
  }
} 